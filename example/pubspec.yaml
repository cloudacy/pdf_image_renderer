name: pdf_image_renderer_example
description: Demonstrates how to use the pdf_image_renderer plugin.
publish_to: "none"

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  file_picker: ^1.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  pdf_image_renderer:
    path: ../

flutter:
  uses-material-design: true
